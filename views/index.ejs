<!DOCTYPE html>
<html>
  <head>
    <title>Home Page</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Home Page</h1>
    <% if (error) { %>
      <p class="error">You are already logged in!</p>
    <% } %>
    <% if (user) { %>
      <p>Hello <%= user.email %>!</p>
      <p><a href="/students">Go to Students</a></p>
      <p><a href="/users/signout">Sign Out</a></p>
    <% } else { %>
      <p>Welcome!</p>
      <p><a href="/users/signin">Sign In</a> | 
        <a href="/users/signup">Sign Up</a></p>
    <% } %>
  </body>
</html>